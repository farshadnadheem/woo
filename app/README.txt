Once the repo is cloned copy the code from default.htaccess, create a .htaccess and paste to it.

Copy the wp-config-sample.php, create a wp-config and paste to it.
In case the DB connection if different from the wp-config; change it and save.

username : Farshad
password : 123